import Box from "@mui/material/Box";
import { useNavigate } from "react-router-dom";

const SignIn = () => {
  // login ui here (use material ui) and use navigate to go to home page

  return <div></div>;
};

export default SignIn;
